<?php 
include 'header.phtml';
?>
<h6 id="mensaje">Has accedido al modo administrador con exito!</h6>

<h2 id="titulo">Presupuestos de construcción:</h2>  

<?php foreach($construcciones as $construccion): ?>
    <div class="separacion">
        <h4 class="identificacion"><?php echo $construccion->identificar; ?></h4>

        <div class="tabla-container">

            <!-- Tabla de Materiales y Costos -->
            <div class="tabla">
                <table>
                    <thead>
                        <tr>
                            <th>descripcion de costo</th>
                            <th>Costo</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php
                        // Mostrar los costos asociados a la construcción
                        foreach ($construccion->costos as $costo): ?>
                            <tr>
                                <td id="descripcion"><?php echo $costo->descripcion; ?></td>
                                <td id="valor"><b><?php echo $costo->valor; ?></b></td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>

            <!-- Tabla de Venta -->
            <div class="tabla">
                <table>
                    <thead>
                        <tr>
                            <th>Venta</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td id="venta"><b><?php echo $construccion->venta; ?></b></td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- Tabla de Certificado -->
            <div class="tabla">
                <table>
                    <thead>
                        <tr>
                            <th>Certificado</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td id="certificado"><b><?php echo $construccion->certificado; ?></b></td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- Tabla de Costo a Pagar -->
            <div class="tabla">
                <table>
                    <thead>
                        <tr>
                            <th>Costo a pagar</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td id="costo_a_pagar"><b><?php echo $construccion->costo_a_pagar; ?></b></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Botón para eliminar la construcción -->
        <a href="eliminar/<?php echo $construccion->id; ?>" type="button" id="btn-construct">Borrar</a>
    </div>
<?php endforeach; ?>
